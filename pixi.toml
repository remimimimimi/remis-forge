[project]
name = "remis-forge"
version = "0.1.0"
description = "Hehe, no"
authors = ["Remis Remimimimimis <valent.xarin@gmail.com>"]
channels = ["conda-forge"]
platforms = ["osx-arm64", "linux-64", "win-64"]

[dependencies]
rattler-build = ">=0.45.0,<0.46"

[tasks]
build = "rattler-build build -c conda-forge --recipe-dir . --skip-existing=all"
